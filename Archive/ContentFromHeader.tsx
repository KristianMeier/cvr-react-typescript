import React from 'react'

const ContentFromHeader = () => {
  return <div>ContentFromHeader</div>
}

export default ContentFromHeader

// @ts-nocheck

{
  /* <SmallLinks>
          {earthIcon.map((item) => (
            <Icon key={item.id} {...item} />
          ))}
        </SmallLinks>

<LinkText>
          {textIcons.map((item) => (
            <StyledLink key={item.id} {...item} />
          ))}
        </LinkText>

const SmallLinks = styled.div`
  display: flex;

  @media (min-width: 768px) {
    display: none;
  }
`

const LinkText = styled.div`
  display: none;

  @media (min-width: 768px) {
    display: flex;
    gap: 1rem;
  }
` */
}
